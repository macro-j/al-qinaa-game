"Act as an expert React and Node.js developer. We need to polish the game loop with auto-advancing phases, ANONYMOUS public vote results, and distinct execution messages.

CRITICAL TASK 1: Auto-Advance Night Actions (Backend index.ts)

In submitNightAction, after successfully recording the action for the active role, clear the current phase timeout.

Immediately invoke the logic to transition to the next phase (e.g., scheduleNextNightPhase or advancePhase). Do not wait for the 20 seconds to run out if the action is already submitted.

CRITICAL TASK 2: Auto-Advance Day Phases (Backend index.ts)

When the phase changes to day_discussion, start a 40-second setTimeout. If it completes, automatically transition the room to the voting phase.

When the phase changes to voting, start a 20-second setTimeout. If it completes, automatically trigger tallyVotesAndExecute.

CRITICAL: If the Host clicks the manual transition buttons early, ensure you clearTimeout the respective timers so they don't double-trigger.

CRITICAL TASK 3: ANONYMOUS Public Vote Results (Frontend & Backend)

Backend: Ensure tallyVotesAndExecute emits the executionResult to the ENTIRE ROOM io.to(code).emit(...), not just the host.

Frontend (PlayerScreen.tsx & HostDashboard.tsx): Update the modal/UI that listens for executionResult. It MUST display to ALL players:

The name of the executed player (e.g., 'تم إعدام: [Player Name]').

The total vote counts ONLY (e.g., '[Player A]: 4 أصوات'). STRICTLY DO NOT reveal who voted for whom. Keep the voters completely anonymous, only show the final tally per targeted player.

CRITICAL TASK 4: Fix 'Peaceful' Message Bug (Frontend PlayerScreen.tsx)

Ensure the morningResults (which handles Night Kills) and executionResult (which handles Day Votes) have STRICTLY SEPARATE UI states.

morningResults should only say 'مرت بسلام' if killedPlayer is null. If someone was assassinated, say 'تم اغتيال [Player Name]!'.

executionResult should NEVER say 'مرت بسلام'. It must strictly announce the execution results based on the anonymous votes.

Execute these 4 quality-of-life improvements carefully."